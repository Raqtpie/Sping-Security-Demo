server:
  port: 9421

spring:
  datasource:
    druid:
      driver: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/security_demo_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
      username: root
      password: 279410

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id